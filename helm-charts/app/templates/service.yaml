apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels: {{ include "app.labels" . | nindent 4 }}
spec:
  selector: {{ include "app.selectorLabels" . | nindent 4 }}
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
